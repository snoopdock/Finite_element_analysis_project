
# ADR — Component Mapping Findings

## Decision

Use existing repository components as implementation foundations.

Do not create duplicate architecture.

## Reason

The repository already contains:

- IR concepts;
- LaTeX processing;
- semantic boundaries;
- validation workflows.

The implementation effort should formalize boundaries rather than replace existing work.
